import path from 'path';

// to get the last portion of our path we can use the basename method
const filepath= path.basename("Pratyush//documents//file.txt")
// this returns the last portion of the path which is file.txt
console.log(filepath);