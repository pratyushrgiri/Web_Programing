import * as os from "os";

// // to know the platform of the os
// console.log(os.platform());
// // to know the artitecture of the system 
// console.log(os.arch());
// // to know the cpu info about the system 
// console.log(os.cpus());
// to find out the host name of the system 
console.log(os.hostname());