const mathmaticalOperations = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,
    power: (a, b) => Math.pow(a, b),
    sqrt: (a) => Math.sqrt(a)
};

module.exports = mathmaticalOperations;